export enum TagType {
  BUTTON = 'button',
  SPAN = 'span',
  DIV = 'div',
  IMG = 'img',
  OL = 'ol',
  LI = 'li',
}

export enum EventListeners {
  CLICK = 'click',
}

export enum ButtonTypes {
  SUBMIT = 'submit',
  BUTTON = 'button',
  RESET = 'reset',
}